name: af2_serverless
channels:
  - conda-forge

dependencies:
  - python=3.9
  - python-dotenv=1.0.0
  # Use pip to install additional packages
  - pip
  - pip:
    - cloudpathlib==0.17.0
    - runpod==1.6.0
  - pip:
      - -e alphafold_MSA
